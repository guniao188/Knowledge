---
title: "25年2月以前部署的18版本，在使用原生导出功能时会遇到报错"
source: "http://www.thinkltd.cn/forum/1/25218-4105"
author:
published: 30 七月 2026
created: 2026-08-17
description: "以MO为例，重现方式为：1.先搜索MO2.全选3.点击操作-导出4.弹窗里点击 产品 这个字段（或者其他many2one类型字段）报错ValueError: Invalid field product.product.state in leaf ('state', 'in', ('draft','done'))问题根源在于，系统把当前界面的domain，也传递给了弹窗里选中的这个字段的模型去搜索，如果此模型里没有这个字段则报错官方也有对应帖子说明https://mergebot.odoo.com/odoo/odoo/pull/193567 [1]如果不打算全量更新模块的话，可以参考以下方法进入源码 /extra/opt/odoo/odoo18/odoo-server/addons/web/static/src/views/list/list_controller.js下图位置 注释let domain = this.model.root.domain;加入let domain = parentParams ? [] : this .model.root.d"
tags:
  - "clippings"
---
**0**

葛忠彪

30 七月 2026

以MO为例，重现方式为：

1.先搜索MO

2.全选

3.点击操作-导出

4.弹窗里点击 产品 这个字段（或者其他many2one类型字段）

报错 ValueError: Invalid field product.product.state in leaf ('state', 'in', ('draft','done'))

问题根源在于，系统把当前界面的domain，也传递给了弹窗里选中的这个字段的模型去搜索，如果此模型里没有这个字段则报错

官方也有对应帖子说明

[https://mergebot.odoo.com/odoo/odoo/pull/193567](https://mergebot.odoo.com/odoo/odoo/pull/193567)

如果不打算全量更新模块的话，可以参考以下方法

进入源码 /extra/opt/odoo/odoo18/odoo-server/addons/web/static/src/views/list/list\_controller.js

下图位置 注释

```
let domain = this.model.root.domain;
加入
  let domain = parentParams ? [] : this.model.root.domain;
```

![](http://www.thinkltd.cn/forum/1/%E5%9B%BE%E7%89%87.png)