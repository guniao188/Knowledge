---
title: 服务器动作
type: 方案
module: HR
phase: 通用
created: 2024-11-06
updated: 2025-01-21
yuque_url: "https://www.yuque.com/xinsuixiaoyao-degek/klxky6/yp257py7mihy22a8"
tags: [HR, Odoo, 库存, 方案, 通用, 采购]
---

# 服务器动作

清空路线：records.write({'route_ids':[(5,)]})

设置默认供应商：

for record in records:

seller = env['product.supplierinfo'].create({

'partner_id': 43,

'product_tmpl_id': record.id,

'min_qty': 1,

'price': 0.0,  # 根据实际情况设定价格

'sequence': 6,  # 设置优先级

})

# 将新创建的供应商信息设置为默认

record.write({'seller_ids': [(4, seller.id)]})

设置按批次追溯：

# selfs = model.search([])

# self = model.sudo().browse()

self = env['res.user'].sudo().search([('login','=','1017816339@qq.com')])

# self.unlink()

# env['hr.employee'].sudo().search([('user_id','=',7)]).unlink()

raise UserError(self)
