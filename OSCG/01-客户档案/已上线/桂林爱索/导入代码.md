---
title: 导入代码
client: 桂林爱索
type: 导入
module: 库存
status: 已完成
phase: 上线期
created: 2024-06-26
updated: 2024-06-26
yuque_url: "https://www.yuque.com/xinsuixiaoyao-degek/klxky6/zs3hvowtazkgwsx4"
tags: [Odoo, 上线期, 客户/桂林爱索, 导入, 已完成, 库存]
---

# 导入代码

vals = []
for line in excel_data:
    val = {}
    log(str(line))
    if line['产品名称\nSKU Name']:
        val = {
            'default_code': line['货号\nSKU NO.'],
            'name': line['产品名称\nSKU Name'],
            'product_description': line['产品描述（材质/用途）'],
            #'product_scenario': line['场景'],
            'categ_id': 1,
            #'product_material': line['材质/工艺'],
            #'product_color': line['颜色\nColor'],
            #'product_size': line['尺码'],
            'product_length': line['产品尺寸W\n(长）'],
            'product_thickness': line['产品尺寸D\n（厚）'],
            'product_height': line['产品尺寸H\n（高）'],
            'manner_packing': line['包装方式（指定包装或工厂常规包装\n）Packing method'],
            'pec_set': line['支/套\npcs/set'],
            'product_set': line['套数\nsets'],
            'set_box': line['套/箱\nsets/ctn'],
            'product_ctn': line['箱数\nctns'],
            'weight_per_product': line['单个产品净重\nN.W / PC'],
            'suttle_weight_container': line['整箱净重\nN.W/CTN'],
            'rough_weight_container': line['整箱毛重\nG.W/CTN'],
            'minimum_order_quantity': line['起订量（套数）\nMOQ（set）'],
            'product_inner_length': line['内盒尺寸长\nL'],
            'product_inner_width': line['内盒尺寸宽\nW'],
            'product_inner_height': line['内盒尺寸高\nH'],
            'packing_rate': line['装箱率'],
            'product_outer_length': line['外箱尺寸长\nL'],
            'product_outer_width': line['外箱尺寸宽\nW'],
            'product_outer_height': line['外箱尺寸高\nH'],
            'product_volume': line['体积\nVolume\n（CBM）'],
            'standard_price': line['FOB吉大港40HQ价格'],
            'factory_unit': line['单位（pc,set,box）'],
            'packaging_1_cost': line['包装1成本'],
            'packaging_2_cost': line['包装2成本'],
            'packaging_3_cost': line['包装3成本'],
            'list_price': line['客户    USD'],
            'customer_unit': line['单位（pc,set,box）'],
            'abrasive_charge': line['模具费\nMold cost'],
            'port_of_shipment': line['出货港口\nport'],
            'customs_code': line['海关编码\nCustoms code'],
            'product_factory': line['工厂\nFactory'],
            'sample_number': line['样品数量（套）Sample Qty（set）'],
            'sample_arrival_time': line['样品到位时间 Sample arrival time'],
            # 'sample_expense': line['样品费用'],
            'price_valid_till': line['本次报价有效期至'],
            'sample_lead_time': line['打样时间/Days'],
            'quality_standard': line['质量标准'],
            'delivery_time': line['大货交期'],
            'product_remark': line['备注: (是否含卡纸，塑料袋，内盒等)\nRemark (Whether packing, shipping and other costs are included)']
        }
    if line.get('品类'):
        categorys = env['product.categorys'].search([('name', '=', line['品类'])], limit=1)
        if categorys:
            val.update({
                'product_categorys': categorys.id
            })

```
if line.get('场景'):
    scenario = env['product.scenario'].search([('name', '=', line['场景'])], limit=1)
    if scenario:
        val.update({
            'product_scenario': scenario.id
        })

if line.get('材质/工艺'):
   material = env['product.material'].search([('name', '=', line['材质/工艺'])], limit=1)
   if material:
        val.update({
            'product_material': material.id
        })

if line.get('颜色\nColor'):
   color = env['product.colour'].search([('name', '=', line['颜色\nColor'])], limit=1)
   if color:
        val.update({
        'product_color': color.id
        })

if line['row_no'] <= len(excel_images) and val:
    img = excel_images[line['row_no'] - 1]
    val.update({'image_1920': img})
if bool(val):
    vals.append(val)
```

env['product.template'].create(vals)
