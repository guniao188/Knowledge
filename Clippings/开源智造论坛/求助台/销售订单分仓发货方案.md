---
title: "销售订单分仓发货方案"
source: "http://www.thinkltd.cn/forum/1/3694"
forum: "求助台"
author: "刘志鹏"
published: 2023-04-24
created: 2026-08-17
tags:
  - clippings
  - odoo
  - 论坛/求助台
---

# 销售订单分仓发货方案

> [!info] 来源
> 开源智造论坛 · 求助台 | 作者:刘志鹏 | 2023-04-24
> <http://www.thinkltd.cn/forum/1/3694>

【业务场景】

某家公司在全国不同地区有多个仓库，每个仓库存放不同种类的产品。

客户下了一份订单，包含两个产品，一个产品在仓库A，一个产品在仓库B。

订单确认后，需要生成两个发货单，分别来自仓库A和仓库B

【实现方法】

1.  在设置 -> 库存中开启多步路线

2.  创建两个仓库华东仓和华北仓，创建两条路线水果路线和零食路线

3. 为水果路线创建一个拉取动作的规则，源位置是华东仓库，目的位置是客户

![[1-3694-d4fb7a66.png]]

```python
    为零食路线创建一个拉取动作的规则，源位置是华北仓库，目的位置是客户

![[1-3694-fe392a41.png]]

```

4. 创建一个产品苹果，在路线中设置为水果路线

```python

![[1-3694-1183a079.png]]

     创建一个产品薯片，在路线中设置为零食路线

![[1-3694-7aadc9fe.png]]

```

5. 新建销售订单，产品包含水果和薯片，订单确认后，生成两张发货单，分别来自华东仓库和华北仓库

![[1-3694-b53b7b15.png]]

![[1-3694-ed4882e8.png]]

## 补充/答案 1

业务场景二：

准备工作：

1. 设置两条路线，华东路线和华北路线，设置两条规则，规则的适用仓库分别是华东仓库和华北仓库

2. 产品苹果和薯片的路线中，两条路线都打上勾

开发方案：

1. 在res.partner模型中新增一个Many2one的字段，关联仓库模型(stock.warehouse)，在视图中显示这个字段

    warehouse_id = fields.Many2one('stock.warehouse', string='Warehouse')

2. 在源码的sale_stock模块中有一个sale_order.py文件，

```python
    它继承了sale.order模型，增加了一个关联仓库的Many2one字段warehouse_id

     这个字段是计算字段，它的计算方法是

    @api.depends('user_id', 'company_id')

    def _compute_warehouse_id(self):

        for order in self:

            default_warehouse_id = self.env['ir.default'].with_company(

                order.company_id.id).get_model_defaults('sale.order').get('warehouse_id')

            if order.company_id and order.company_id != order._origin.company_id:

                warehouse = default_warehouse_id

            else:

                warehouse = self.env['stock.warehouse']

            if order.state in ['draft', 'sent']:

                order.warehouse_id = warehouse or order.user_id.with_company(order.company_id.id)._get_default_warehouse_id()
```

            # In case we create a record in another state (eg: demo data, or business code)

```python
            if not order.warehouse_id:

                order.warehouse_id = self.env.user._get_default_warehouse_id()
```

3. 重写此方法， warehouse_id的先从Sale Order的partner_id取，取不到再走已有的逻辑

@api.depends('user_id', 'company_id', 'partner_id')

*def* _compute_warehouse_id(self):

    *for* order *in* self:

```python
        default_warehouse_id = self.env['ir.default'].with_company(

            order.company_id.id).get_model_defaults('sale.order').get('warehouse_id')
```

        *if* order.partner_id *and* order.partner_id.warehouse_id:

            warehouse = order.partner_id.warehouse_id

        *elif* order.company_id *and* order.company_id != order._origin.company_id:

            warehouse = default_warehouse_id

        *else*:

            warehouse = self.env['stock.warehouse']

        *if* order.state *in* ['draft', 'sent']:

```python
            order.warehouse_id = warehouse *or* order.user_id.with_company(

                order.company_id.id)._get_default_warehouse_id()
```

        # In case we create a record in another state (eg: demo data, or business code)

        *if not* order.warehouse_id:

            order.warehouse_id = self.env.user._get_default_warehouse_id()

4. 创建一个山东客户，仓库选择为华东仓库

    创建一个北京客户，仓库选择为华北仓库

![[1-3694-427e8ed5.png]]

5. 创建销售订单，客户为山东客户，产品包含了苹果和薯片

    确认后生成了一张发货单，来自华东仓库

![[1-3694-14b73fbd.png]]

```python
    创建销售订单，客户为北京客户，产品包含了苹果和薯片

    确认后生成了一张发货单，来自华北仓库

![[1-3694-4d96db41.png]]

```

## 补充/答案 2

【业务场景二】

1.  假设在华东仓和华北仓都有货，即两个仓库都有零食和水果。希望实现的是，根据客户的发货地址决定从华北发，还是从华东发（可以考虑在客户的发货地址，也即收货联系人上增加一个仓库字段，onchange时候设置SO上的warehouse_id字段值）。
2.  此种场景，要二次开发实现，请说明二次开发要点，哪个模块，哪个文件，哪个方法，修改哪些代码？指明开发要点即可，不必实际开发。可以直接修改Odoo源码验证开发方案。

【二开要点】

---

相关:[[Clippings/开源智造论坛/求助台/00-求助台索引.md|← 求助台索引]]
